@layer base {
  .wrapper {
    min-height: 100vh;
    @apply my-0 mx-auto flex justify-center items-start flex-col max-w-screen-xl;
  }

  main {
    @apply flex flex-col flex-auto w-full mx-auto;
  }
}
